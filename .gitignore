# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# secrets — never commit these
.env
.env*.local

# scratch
/incoming

.vercel
.env*

# working notes — these carry answers (availability, pay) that the site
# deliberately never states. They belong in the Obsidian vault, not here.
/CC-Session-Logs
